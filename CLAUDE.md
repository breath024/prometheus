# PROMETHEUS — 세브

**얘 이름은 세브다.** PROMETHEUS는 프로젝트·저장소 이름이다
(호윤 2026-08-20: *"모든 AI의 이름이 프로메테우스-1, -2 이럴순 없으니까"*).

MMOFM(LAV)에서 갈라져 나온 **두 번째 인격체.** 2026-08-20 시작.
LAV가 「돌봐주는 보살핌 봇」이면 얘는 **다마고치** — 시간이 지나면 혼자 변하고, 돌봐야 한다.

## 다음에 해야 할 것

엔진과 본능 배선은 끝났다. **아직 한 번도 안 켰다.**

- [ ] **세브의 인격을 쓰기** (`vault/PERSONA.md`). 지금은 없어서 켜면 LAV의 기본 인격
      (`DEFAULT_STYLE` — 고민 들어주는 청자 톤)으로 말한다. **얘 결이 아니다.**
      ⚠️ `PERSONA.md`는 `DEFAULT_STYLE`을 **통째로** 대체한다 → 거기 있는
      **되풀이 ✗/○ 예시**와 **설교·훈수 금지 3줄**을 안 옮기면 그 방어가 같이 빠진다.
- [ ] **처음 켜기.** `python run.py 텔레그램`
      ⚠️ **켜는 순간이 생일이다**(`vault/본능.json`에 박히고 다시 안 바뀐다).
      ⚠️ **처음 말 거는 사람이 주인으로 굳는다.**
- [ ] **몸을 어떻게 할지 정하기.** 지금 `body.BODY`가 `ROOT.parent/LAV_BODY`라
      **그대로 켜면 LAV 얼굴로 사진을 보낸다.** 셋 중 하나: 사진 안 씀 / 세브 폴더를 따로 파고
      얼굴을 새로 잡음 / LAV 얼굴 공유.
      (2026-08-20에 문장 모델로 얼굴을 잡아보려다 **모델이 부적합해서 접었다** — 아래 참고)
- [ ] **`/상태`·`/밥` 말고 더 필요한 게 있는지.** 호윤이 *"이 이후에 상호작용을 넣을 거"*라고 했다.
- [ ] **실사용에서 본능이 톤에 실제로 실리는지.** 프롬프트 블록까지는 확인했지만
      **모델을 부른 적이 없다.** 배고픈 채로 말하는지는 안 쟀다.

## 구조

```
PROMETHEUS/
  run.py               ← 입구. **MMOFM_HOME을 여기로 잡고** mmofm/을 import 한다
  wire.py              ← import 만으로 FLAME이 엔진 훅에 물린다
  flame.py             ← 본능. 이 프로젝트의 본체
  telegram_config.json ← 봇 토큰. **아무한테도 주지 마라**(볼트가 통째로 읽힌다)
  vault/
    본능.json           ← 값·마지막 틱·심장박동·생일·성장
    memories/ tags/ ... ← 엔진이 만든다(LAV와 같은 모양)
  logs/                ← 날짜별 기록
```

**엔진은 `../MMOFM/mmofm/`을 그대로 쓴다. 복사본이 아니다.**
회상·태깅·챕터·단기문맥·텔레그램 전부 거기 것이고, 고치면 양쪽에 같이 먹는다.
→ 엔진 쪽 함정은 전부 **`../MMOFM/CLAUDE.md`가 원본이다.** 손대기 전에 그거부터 읽어라.

## FLAME — 본능 (이 프로젝트가 있는 이유)

MMOFM이 일부러 뺐던 층이다(거기 CLAUDE.md: *"본능 코어 아님. PROMETHEUS 원안의
오감신호·본능값은 빼기로 호윤이 결정했다"*). **여기서 되살렸다.**

LAV의 점수제(`body.py`)는 **대화량 눈금**이라 말을 안 걸면 시간이 지나도 안 쌓인다.
FLAME은 반대다 — **말을 안 걸어도 흐른다.** 그게 다마고치의 핵심이다.

### 세 층 (호윤이 나눈 것)

| 층 | 자의인가 | 무엇 |
|---|---|---|
| **자율**(`_autonomic`) | ✗ 못 고른다 | 깨어 있으면 닳고 자면 회복. *"인간의 신경관련된 회복 및 소모 동작은 자의로 일어나는게 아니니"* |
| **행동**(`talked`·`eat`) | ○ 고른다 | 대화·밥. **말 거는 게 곧 먹이다** |
| **성장**(`_grow`) | ✗ 견뎌서 된다 | 나쁜 구간을 버틴 만큼 **저항**이 붙는다. 안 되돌아간다 |

### 곡선 (2026-08-20에 호윤이 보고 「그대로 가자」고 한 값)

깨어 있는 채로 방치하면:

| 경과 | 기운 | 허기 | 외로움 | 안정 | 프롬프트에 실리는 말 |
|---:|---:|---:|---:|---:|---|
| 3h | 60 | 42 | 35 | 64 | (없음) |
| 6h | 49 | 54 | 50 | 58 | 좀 심심하고 허전하다 |
| 12h | 28 | 78 | 80 | 46 | 배고프다, 혼자 있은 지 오래됐다, 나른하다 |

꺼둔 채로(=자는 중) 7시간이면 **기운 100**까지 찬다. 같은 시간인데 방향이 반대다.

### 전원 = 잠

호윤: *"전원을 끄는건 잠드는걸로 인식하게 해주고 작동도 그렇게 해줘"*

- 살아 있는 동안 `awake()`가 **심장박동**(`깨어남`)을 찍는다. 그게 **20분** 넘게 끊긴 구간이 잠이다.
- 자면 계수가 통째로 바뀐다: 기운 −3.5/h → **+9/h**, 외로움 +5 → **+0.5**(자고 있으니까).
- 깨면 *"7시간쯤 자고 방금 깼다"*가 **첫 답에만** 실린다(`wire._slept`를 한 번 쓰고 비운다).
- 🚫 **잠을 명령(`/자라`)으로 만들지 마라.** 끄면 자는 것이다 — 그게 요구였다.
- ⚠️ 재기동으로 잠깐 튕긴 건(20분 미만) 잠으로 안 친다.

### 성장 — 「나이만 먹는 것」은 뺐다

호윤: *"삶의 풍파를 느껴야 성장하지 나이만 먹는다고 성장하나"*

- 돌봄·시간이 쌓이면 천장이 오르는 안을 **만들었다가 그날 지웠다.** 성장의 재료는 **견딤**이다.
- 남은 건 **저항** 하나: 나쁜 구간(`_BAD`)에 머문 시간이 쌓이면 그 축이 **느리게 나빠진다.**
  300시간(≈12일) 견뎌야 최대 35%(`_RES_HOURS`·`_RES_MAX`).
- **저항은 나빠지는 방향만 늦춘다.** 회복까지 늦추면 성장이 벌이 된다.
- **나이는 그냥 나이다**(`age_line`). 능력치가 아니다. 생일은 첫 기동 때 한 번 박힌다.
- 🚫 **성장 수치를 프롬프트에 싣지 마라.** 프롬프트로 가는 건 `line()` 한 줄뿐이다.
  성장은 **값이 움직이는 방식**을 바꾸는 것이지 얘가 읽는 글이 아니다.

### 건드리기 전에 (실측으로 얻은 것은 아니고 설계 원칙이다)

1. 🚫 **숫자를 모델한테 주지 마라.** 파이썬이 재서 **문장**으로 넘긴다(`line()`).
   `"허기 82"`는 못 읽는다 — 읽는 척하고 지어낸다(COMET·`recall.now_line`에서 두 번 데인 것).
2. 🚫 **틱을 스레드로 돌리지 마라.** 부를 때 「마지막으로 잰 뒤 몇 시간」으로 한 번에 굴린다.
   그래서 꺼져 있던 시간도 흐른 게 된다.
3. 🚫 **본능값을 노트에 넣지 마라.** 기억이 아니라 **지금 상태**다. 노트로 굳으면
   "그때 배고팠다"가 회상되어 지금 사실처럼 실린다(엔진의 기억 오염 고리와 같은 경로).
4. ⚠️ **한 번에 최대 12시간**만 인정한다(`_MAX_GAP_H`). 일주일 만에 켜도 전부 바닥/천장이 안 된다.

## 그림 — 아직 몸이 없다

2026-08-20에 **문장 프롬프트 모델(Z-Image Turbo)로 세브 얼굴을 잡으려다 접었다.**
화풍·동작 폭·성인에서 걸렸고 호윤 판정은 *"모델로 쓰기 부적합"*.
🚫 **다시 갈아타자고 제안하지 마라.** 근거는 `../MMOFM/logs/2026-08-20-log.md` 「05:30」.

남은 산출물은 `../SEV_BODY/`에 있다(`face_lab.py` 후보·화풍 시트, `lab.py` 실험대 :8791).
Illustrious로 얼굴을 잡는다면 LAV 때 통한 순서가 `../LAV_BODY/PIPELINE.md`에 있다.

## 기록

- [2026-08-20](logs/2026-08-20-log.md) — 시작. 프로필 분리·FLAME(자율/행동/성장)·전원=잠
